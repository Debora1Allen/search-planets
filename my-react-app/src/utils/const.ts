import { ColumnOptions } from './types';

export const columnFields = ['population', 'orbital_period',
  'diameter', 'rotation_period', 'surface_water'] as ColumnOptions[];
export const comparisoFields = ['maior que', 'menor que', 'igual a'];